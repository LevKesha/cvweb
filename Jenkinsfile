pipeline {
agent any
    stages {
        stage('one'){
        steps {
        sh '/usr/bin/sudo apt-get install docker'}
        }
        stage('two'){
        steps {
        echo 'great'}
        }
    }
    }