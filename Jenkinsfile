pipeline {
agent any
    stages {
        stage('one'){
        steps {
        sh 'cd /var/jenkins_home/workspace/cv'}
        }
        stage('two'){
        steps {
        sh 'sudo apt-get install docker'}
        }
    }
    }